document.getElementById('f').addEventListener('submit',async e=>{e.preventDefault();const r=document.getElementById('r');r.textContent='Creating delivery...';const body={customerName:customerName.value.trim(),customerPhone:customerPhone.value.trim(),customerEmail:customerEmail.value.trim(),address:address.value.trim(),itemDescription:itemDescription.value.trim(),barcode:barcode.value.trim(),paymentMethod:paymentMethod.value};try{const x=await fetch('/api/deliveries',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),d=await x.json();if(!x.ok)throw Error(d.error);r.className='message success';r.innerHTML=`Created <b>${d.delivery.delivery_code}</b>. <a href="/dispatcher.html">Open dispatcher</a>`;e.target.reset()}catch(err){r.className='message error';r.textContent=err.message||'Unable to create delivery'}});
