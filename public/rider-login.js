async function load(){try{const x=await fetch('/api/riders'),rs=await x.json();rider.innerHTML='<option value="">Select your rider</option>'+rs.map(r=>`<option value="${r.id}">${r.name} — ${r.fleet_no} — ${r.plate_no}</option>`).join('')}catch(e){msg.className='error';msg.textContent=e.message}}login.addEventListener('submit',e=>{e.preventDefault();if(!rider.value){msg.className='error';msg.textContent='Select a rider.';return}sessionStorage.setItem('riderId',rider.value);location.href='/rider.html'});load();
